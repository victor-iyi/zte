"""Supervised / auxiliary targets and external vector spaces.

- `zte.data.targets.meaning` -- sentence "meaning" matrices (E5 / Qwen).
- `zte.data.targets.text` -- sentence text embeddings and hard-negative mining.
- `zte.data.targets.tokens` -- tokenised reference sentences for the prefix decoder.
- `zte.data.targets.glove` -- GloVe provisioning for word-level targets.
- `zte.data.targets.behaviour` -- the behaviour (eye-tracking summary) matrix.
- `zte.data.targets.categories` -- sentence category / length-band labels.
"""

from __future__ import annotations

__all__: list[str] = []
